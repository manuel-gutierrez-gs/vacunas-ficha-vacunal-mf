import { css, CSSResult, CSSResultOrNative } from 'lit';

export class SticErroresTheme {
  static readonly cssBase: CSSResult = css``;

  static readonly SticErroresTheme: CSSResultOrNative[] = [SticErroresTheme.cssBase];
}
